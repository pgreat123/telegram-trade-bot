# secrets — never commit these
.env
*.session
*.session-journal

# state / logs — machine-specific, shouldn't be in git history
logs/*.json
logs/*.jsonl

# python
__pycache__/
*.pyc
venv/
.venv/
*.egg-info/

# OS
.DS_Store